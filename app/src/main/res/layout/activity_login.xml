<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="ar.uba.fi.nicodiaz.mascota.LoginActivity">

    <include
        android:id="@+id/toolbar"
        layout="@layout/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingLeft="24dp"
        android:paddingRight="24dp"
        android:paddingTop="56dp">

        <Button
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="24dp"
            android:background="@drawable/com_facebook_button_background"

            android:drawableLeft="@drawable/facebook_logo"
            android:onClick="onFacebookLoginClick"
            android:padding="12dp"

            android:text="@string/facebook_label"
            android:textColor="@color/com_facebook_share_button_text_color"
            android:textSize="@dimen/com_facebook_share_button_text_size"
            android:textStyle="normal" />

        <Button
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="24dp"
            android:background="@drawable/button_drawable"

            android:drawableLeft="@drawable/app_logo_btn"
            android:onClick="onApplicationLoginClick"
            android:padding="12dp"

            android:text="@string/app_name"
            android:textColor="@color/com_facebook_share_button_text_color"
            android:textSize="@dimen/com_facebook_share_button_text_size"
            android:textStyle="normal" />

    </LinearLayout>


</LinearLayout>